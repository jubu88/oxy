// A subtle light animated background — soft pastel aurora blobs that slowly drift.
// Pure CSS (see .aurora in oxy.css); sits behind the content over the off-white.
export function Background() {
  return <div className="aurora" aria-hidden="true" />;
}
