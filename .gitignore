node_modules/
dist/
*.local
# secrets — never commit API keys (Stitch/Gemini etc.)
*.key.local
# downloaded GGUF models are large — never commit
models/
# per-build project workspaces + engine checkpoints
workspace/projects/*
!workspace/projects/.gitkeep
# logs / run artifacts
*.log
last-run.json
skillopt/last-opt.json
# continuous-improvement journal (real-build supervisor reviews) — local signal
skillopt/journal.jsonl
# A/B run artifacts + frozen pre-optimization skill copy
skillopt/ab-report/
skill/system.seed.md
Thumbs.db
.DS_Store
