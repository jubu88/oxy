{
  "name": "oxy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Build web apps with a local LLM. Simple, beautiful, just works.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --test agent/loop.test.ts"
  },
  "dependencies": {
    "node-llama-cpp": "^3",
    "react": "^19",
    "react-dom": "^19"
  },
  "devDependencies": {
    "@types/node": "^24",
    "@vitejs/plugin-react": "^4",
    "typescript": "^5",
    "vite": "^6"
  },
  "optionalDependencies": {
    "playwright": "^1"
  }
}
