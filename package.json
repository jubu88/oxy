{
  "name": "oxy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Build web apps with a local LLM. Simple, beautiful, just works.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --test agent/loop.test.ts engine/node-llama-map.test.ts engine/openai-map.test.ts",
    "oxy": "node driver/run-build.ts",
    "serve": "node server/serve.mjs"
  },
  "dependencies": {
    "node-llama-cpp": "^3",
    "react": "^19",
    "react-dom": "^19"
  },
  "devDependencies": {
    "@types/node": "^24",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^4",
    "typescript": "^5",
    "vite": "^6"
  },
  "optionalDependencies": {
    "playwright": "^1"
  }
}
